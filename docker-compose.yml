
services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: cookflow_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - cookflow_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d cookflow_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ../cookflow-backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      DJANGO_ALLOWED_HOSTS: "frontend,backend,localhost,127.0.0.1"
      DJANGO_CORS_ALLOWED_ORIGINS: "http://frontend:80,http://localhost:5173,http://127.0.0.1:5173"
    volumes:
      - ../cookflow-backend:/app
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    environment:
      VITE_API_URL: http://backend:8000/api
      CYPRESS_BASE_URL: http://frontend:80
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      - backend

  cypress:
    image: cypress/browsers:latest
    command: sh -c "npm install && npm run cypress:run"
    environment:
      CYPRESS_BASE_URL: http://frontend:80
      CYPRESS_API_URL: http://backend:8000/api
    volumes:
      - .:/e2e
      - /e2e/node_modules
    working_dir: /e2e
    depends_on:
      - frontend
      - backend
    restart: "no"

volumes:
  cookflow_db_data: